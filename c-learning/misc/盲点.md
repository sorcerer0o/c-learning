// 位运算原理 
// && 和 ||的原理和使用
// 位运算找出数组中出现一次的元素
for(int i = 0; i < len; i ++) {
    return ^= arr[i];
}
// 位运算找出数组中两个出现一次的元素
int res = 0;
for(int i = 0; i < len; i++) {
    res ^= arr[i];
}
int lsb = res & (-res);
int num1 = 0;
int num2 = 0;
for(int i = 0; i < len; i++) {
    if ( arr[i] & lsb) {
        a ^= lsb;
    } else {
        b ^= lsb;
    }
}
// 循环嵌套不熟练
// break、return、 continue的界定；
// 发牌问题
// 掷骰子问题
// 数组问题第二题
// 帕斯卡三角形
// 递归实现汉诺塔问题
// 大端存储和小端存储
大端存储：数据低位占高地址；
小段存储：数据低位占低地址；
//虚拟内存的定义和优势
虚拟内存是从0到最大值的连续内存；
1.进程间隔离；
2.避免内存空间碎片化；
3.使用内存麻烦
// 虚拟内存的地址是什么；
虚拟内存地址：虚拟内存地址就是虚拟空间上，一个位置的唯一性标识，一个字节来的
变量地址：???
// 小端存储的作用
1.类型转换/指针强转方便；
2.运行效率高
// 指针/指针变量
指针是地址；指针变量是存储地址的
// 指针的关键
指针是存地址的，指针指向的这块内存一定要是有效的内存
// int (*arr)[]???
数组指针
