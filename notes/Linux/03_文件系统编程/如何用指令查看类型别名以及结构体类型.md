> ~(Gn!)~
>
> 当然，如果我们不省略在 vim 中查看文件的过程，我们就能获得更完整的视图，明白这些类型是如何在实际的文件中被定义和使用的。这个步骤对于理解和确认 `grep` 命令找到的信息的上下文非常重要。下面是整个过程的详细步骤，包括在 vim 中查看文件的内容：
>
> 1. **开始查找 `mode_t` 的定义**：
>    - 使用 `grep` 搜索 `mode_t` 在 `/usr/include/` 目录中的定义：
>      ```bash
>      grep -rn "typedef.*\<mode_t" /usr/include/
>      ```
>    - 这一步找到了多个文件中 `typedef __mode_t mode_t;` 的声明，这意味着 `mode_t` 是 `__mode_t` 的别名。
>
> 2. **深入查找 `__mode_t` 的引用和定义**：
>    - 继续搜索 `__mode_t` 的所有引用和定义：
>      ```bash
>      grep -rn "\<__mode_t" /usr/include/
>      ```
>    - 这些结果显示 `__mode_t` 在多个地方被定义和引用，提示了 `__mode_t_defined` 这个宏用于防止重复定义。
>
> 3. **查看 `__mode_t` 在 `types.h` 中的定义**：
>    - 直接在 vim 中打开 `types.h` 文件，查看第150行，以确认 `__mode_t` 的定义：
>      ```bash
>      vim +150 /usr/include/x86_64-linux-gnu/bits/types.h
>      ```
>    - 在 vim 中，您将看到类似于 `__STD_TYPE __MODE_T_TYPE __mode_t; /* Type of file attribute bitmasks. */` 的声明，这是一个基于宏的类型声明。
>
> 4. **确定 `__MODE_T_TYPE` 的定义**：
>    - 查找 `__MODE_T_TYPE` 的定义：
>      ```bash
>      grep -rn "__MODE_T_TYPE" /usr/include/
>      ```
>    - 这一步揭示了 `__MODE_T_TYPE` 被定义为 `__U32_TYPE`。
>
> 5. **查找 `__U32_TYPE` 的基本类型**：
>    - 最终确认 `__U32_TYPE` 的具体类型：
>      ```bash
>      grep -rn "__U32_TYPE" /usr/include/
>      ```
>    - 这一步确定了 `__U32_TYPE` 在系统头文件中定义为 `unsigned int`。
>
> 结论
>
> 通过这个详尽的步骤，您完整地追踪了从 `mode_t` 到其底层类型 `unsigned int` 的完整定义路径。在 `vim` 中查看具体文件和行号是一个重要的步骤，因为它为您提供了一个可视化和直接的方式来查看和验证源代码中的类型定义。这个过程展示了如何通过系统地使用 `grep` 和文本编辑器来理解和解码复杂的类型定义在Linux系统开发中的应用。

> ~(Gn!)~
>
> 查询结构体的过程：
>
> 知道结构体的名字，然后`grep -rn "struct timespec {" /usr/include/`
>
> 一般就可以找到了