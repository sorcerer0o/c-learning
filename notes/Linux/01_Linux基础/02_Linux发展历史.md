###### <sub><font color = orange>王道C++班级参考资料</font></sub><br />——<br />Linux部分<sup><font color=white>卷1</font></sup>Linux基础<br/><sup><sub><font color=cyan>节2</font></sub><font color=cyan>Linux发展历史</font></sup><br/><br/>*最新版本`V3.0`*<br>**王道C++团队**<br/>*COPYRIGHT ⓒ 2021-2024. 王道版权所有*

[TOC]

# 前言

> _~Gn!~_
>
> 从今天开始，我们要讲解Linux操作系统的相关知识概念，于是我们就有了一个基础的疑问：
>
> 什么是操作系统呢？
>
> 我们先从术语的角度来解释计算机操作系统：
>
> 计算机操作系统（Operating System，简称OS）是一种管理计算机硬件资源，提供进程运行环境的<font color=red>**系统软件**</font>，并且操作系统还作为用户和计算机硬件之间的接口。
>
> 如果你不甚理解这句话，也没有关系，在随后的课程内容中你会逐渐理解这句话。
>
> 在整个操作系统的发展历程中，Linux操作系统无疑具有举足轻重的地位。从其诞生之初的1991年起，Linux已经从最初林纳斯·托瓦兹（Linus Torvalds，Linux之父）的个人项目，演变成为支撑全球计算基础设施的核心力量。
>
> 在实际的应用中，Linux是服务器端的霸主级操作系统，在移动端，安卓（Android）同样基于Linux内核开发，而且Linux的成功还促进了开源软件运动的兴起，为全球范围内的开源软件合作开发树立了标杆。
>
> 接下来我们的课程就要来深入学习一下Linux操作系统以及C语言相关的一些技术栈。
>
> 那么首先，我们先来大致了解一下Linux的发展历程。

# Linux的诞生

> _~Gn!~_
>
> <span style=color:red;background:yellow>**1960年代的Multics System：**</span>
>
> 早期的操作系统为批处理操作系统，输入设备只有卡片阅读机，输出设备只有打印机。程序员的编程是在读卡纸上进行的，通过打孔机进行打孔，打孔表示 1，没打孔表示 0。
>
> 这样的操作系统缺陷很明显：如果计算机需要处理多个任务，那么后面的任务就需要排队执行，浪费了CPU的性能。
>
> 进入到1960年代初期，麻省理工学院 (MIT) 开发了划时代的兼容分时系统 CTSS (Compatible Time-Sharing System)，它允许操作系统在多个任务之间来回切换，于是多个用户可以通过终端连接到主机，共享计算资源。
>
> 兼容分时操作系统的主要优势就在于：它让每个用户感觉是在独享主机的资源，在计算资源非常宝贵的年代，这是非常难得的。然而，即便是当时最先进的主机，也只能支持大约30个终端机，这还是有些不够的。
>
> 为了提升大型主机的资源利用率，并使更多用户能够同时使用这些资源，贝尔实验室（Bell Labs）、麻省理工学院和通用电器公司（GE）于1965年联合启动了Multics（Multiplexed Information and Computing Service，多路信息计算系统）计划。
>
> Multics System是一种全面通用的分时操作系统，目标是让大型主机能够同时支持300多个终端机。然而到了1969年，由于项目进展缓慢和资金短缺，贝尔实验室认为Multics项目不可能成功，因此决定退出。
>
> Multics 计划其实并不算失败，在后续它也取得了不俗的成就，并直到1980年代才完全被搁置终止。不过这个我们且按下不表，因为我们要讲另一个更加波澜壮阔的历史。
>
> <span style=color:red;background:yellow>**1969年：Ken Thompson 发明了 Unics**</span>
>
> 1969年，贝尔实验室退出了宏大且复杂的Multics计划，Ken Thompson和Dennis Ritchie等曾参与该项目的工程师回到了贝尔实验室。由于比较空闲，这一年 Ken Thompson 写了一个游戏 —— 太空旅行 (Space Travel)。
>
> 然而，由于贝尔实验室当时仍在使用不太适合交互式使用的批处理操作系统，这款游戏在实际体验上并不理想。
>
> 机缘巧合之下，Thompson在实验室的库房里发现了一台被搁置的PDP-7小型计算机。他决定将游戏移植到这台设备上。但移植过程中，他发现游戏在PDP-7上运行效率低下。这激发了Thompson对为这台机器编写新操作系统内核的想法。
>
> 恰好在同年的8月份，Thompson 获得了一个月的休假期，而且他的妻子孩子也回老家探亲了。于是 Thompson 就利用这一个月时间，以及他从 Multics 项目中获得的灵感，将庞大而复杂的 Multics System 做了大幅地简化。实验室的同事也因此戏称这个系统为 Unics，寓意为更简洁的Multics。
>
> Thompson在设计 Unics 系统内核时，有两个重要的思想：
>
> 1. <font color=red>**首先是“一切皆文件”原则。**</font>所有的程序或者系统设备都可以被视为文件。
> 2. <font color=red>**其次是"高效简洁"的原则。**</font>即程序应仅仅专注于有效地完成其既定目标。
>
> 这些理念后来被称为KISS原则 (Keep It Simple, Stupid)，这也是 "Unix 哲学"的根本原则。有一本著作叫《The Art of Unix》就是讲 Unix 的设计原则的。
>
> **当然这一年还发生了一件大事，在地球另一端的——芬兰，Linux之父林纳斯（Linus Travalds）出生了。**
>
> <span style=color:red;background:yellow>**1973年：Unix 系统内核正式诞生**</span>
>
> 由于 Thompson 写的 Unics 操作系统实在太好用了，在贝尔实验室内部广为流传，并且经过多次改版。
>
> 但是因为 Unics 是以汇编语言编写的，高度依赖硬件，而且当时计算机机器架构都不太相同，所以每次安装 Unics 系统，都要重新修改汇编代码，非常的不方便。
>
> 于是 Thompson 与 Dennis Ritchie 合计将 Unics内核用高级语言进行重写，从而提高操作系统的可移植性。他们先选择了 BCPL ，然后又尝试PASCAL，发现编译出来的内核性能都不是很好。
>
> 于是，两人 (主要是Dennis Ritchie) 决定自己开发一套新的语言：这就是大名鼎鼎的 C 语言。<font color=red>**C语言之父一般认为是Dennis Ritchie。**</font>
>
> 1973年，Dennis Ritchie 用 C 语言重写了 Unics 的内核，随后发行出 Unix 的正式版本。 
>
> 重写说起来简单，实际上并不是简单的汇编到C 语言的翻译，这里边包含了很多的发明和创造。比如，在此期间引入的 “pipe” (管道) 功能后来成为了 Unix 的一大优势。而管道的概念也是由贝尔实验室的 Douglas McIlroy (当时 Bell Lab 的主管) 发明的。
>
> <span style=color:red;background:yellow>**1977年：Unix 的重要分支 BSD 诞生**</span>
>
> 1977年是Unix历史上的关键一年，这一年，Unix的一个重要分支——BSD（Berkeley Software Distribution，伯克利软件套件）诞生了。
>
> Unix的早期版本由AT&T（美国国际电话电报公司）的贝尔实验室开发，贝尔实验室当时隶属于AT&T公司。Unix诞生之初，AT&T采取了相对开放的策略，允许学术界获取Unix的内核源码。这种开放的态度促进了Unix与学术界的紧密合作，尤其是与加州大学伯克利分校（UC Berkeley）的合作。
>
> 在这个过程中UC Berkeley的**研究生Bill Joy**获得了Unix的内核源码，并开始着手修改，以使其更适合自己的机器。
>
> 在此过程中，他增加了许多新的工具软件和编译程序。经过这些改进和增强，他最终将这个版本命名为Berkeley Software Distribution，简称BSD。BSD不仅是Unix的一个重要分支，也为后续的操作系统开发提供了众多创新和灵感。
>
> 值得一提的是，Bill Joy不仅因BSD成为计算机科学界的重要人物，他后来还参与创立了Sun Microsystems公司，进一步影响了整个计算机行业。Sun公司是Java编程语言的发明者，这同样是一个计算机领域举足轻重的编程语言。
>
> <span style=color:red;background:yellow>**1979年：AT&T 发布支持 x86 架构带商业版权的 System V 第七版**</span>
>
> 1979年是Unix历史上的一个里程碑年份，这一年，AT&T发布了支持x86架构的个人计算机（PC）操作系统System V第七版，为Unix操作系统的商业发展开辟了新的道路。
>
> 由于 Unix 的高度可移植性与强大的性能，加上当时没有版权纠纷，很多商业公司开始了 Unix 操作系统的开发。 他们将 Unix 操作系统移植到自家的硬件平台上，从而推出适合自家硬件平台的 Unix 系统，例如 AT&T 的 System V、IBM 的 AIX，HP 的 HP/UX 等等...
>
> 操作系统内核 (Kernel) 必须跟硬件密切配合，因为操作系统需要管理硬件资源。早期生产计算机硬件的公司还没有统一的标准，每一个公司生产的硬件都不相同。因此每一个公司必须为自己的计算机硬件开发合适的 Unix 系统。而且，每一个公司开发出来的 Unix 系统是不能在其它硬件架构下工作的。另外，由于没有厂商针对个人计算机设计 Unix 系统 (因为那时个人计算机并不流行)。因此，在当时是没有支持个人计算机的 Unix 系统的。
>
> 直到 1979 年，AT&T 推出了 System V 第七版，这个情况才有点改善。这一版最主要的特色是：可以支持 x86 架构的个人计算机。也就是说个人计算机终于可以用上 Unix 系统了。
>
> 到了80年代初，曾经风光无限的AT&T公司开始被美国当局进行反垄断调查并最终被强制拆分，出于公司生存考虑，AT&T公司开始将Unix操作系统进行商业化，也就是收费并且不再提供源代码。
>
> AT&T公司的这一操作，瞬间制造了 Unix 业界的紧张气氛，同时也点燃了商业纠纷的战火。
>
> 这又是另一个波澜壮阔的历史，感兴趣的同学可以自行查看相关资料。目前存活下来，且还在被商业使用的大概就只有 System V 和 BSD 这两系了。
>
> <span style=color:red;background:yellow>**1984年：Minix 系统，GNU 项目**</span>
>
> 1984年，在Unix界发生了两件具有深远影响的事件：Minix系统的诞生和GNU项目的启动。
>
> AT&T的System V第七版的版权声明对学术界造成了巨大的影响，尤其是影响了教授操作系统的教育工作者。因此，Andrew Tanenbaum教授，一位在荷兰阿姆斯特丹自由大学的计算机科学家，决定编写一个类Unix的操作系统，用于教学目的。
>
> 为了避免版权纠纷，Andrew Tanenbaum 完全不看 Unix 的内核源码，编写了一个完全Mini 版本的 Unix 系统内核，所以被称为 Minix。
>
> 1986 年，Minix完成编写任务，并于次年出版书籍《Operating Systems: Design and Implementation》(书的附录中有 Minix 的源码)。Tanenbaum 还有另一本很出名的书籍《Modern operating systems》。
>
> Minix 并不是免费的，必须通过磁盘磁带进行购买。不过售价非常便宜，而且还会随赠 Minix 源码。
>
> Minix 系统收到了广大用户的欢迎，很多用户强烈要求 Andrew Tanenbaum 对 Minix 进行改进。但是 Andrew Tanenbaum 教授只是把 Minix 定位在教育用途上面，无心对Minix 进行持续开发。这才有了后面 Linux 的故事...
>
> 在 Andrew Tanenbaum 教授开始编写 Minix 的1984年，一个伟大的计划正在悄然诞生，这就是日后和 Linux 双剑合璧一起改变世界的GNU 项目。
>
> GNU 项目，是一个自由软件集体协作项目，它是由 MIT 的 Richard Mathew Stallman (RMS) 发起的，它的目标是创建一套完全自由开源、免费的 Unix 操作系统。GNU 是 "Gnu is Not Unix" 的递归缩写。
>
> 1985 年，为了避免 GNU 所开发的自由软件被其他人所利用而成为专利软件， 所以 Richard Stallman 与律师草拟了有名的通用公共许可证 (General Public License, GPL)，并且称呼为 copyleft (相对于专利软件的 copyright)。
>
> GNU 项目领导了计算机产业轰轰烈烈的自由软件运动，该项目也开发出了许多高质量的免费软件，其中包括有名的 Emacs 编辑系统、Bash Shell 程序、GCC 系列编译程序、GDB 调试程序等等。
>
> 不过，对于 GNU 的最初构想『建立一个自由的 Unix 操作系统』来说，有这些优秀的程序仍然是不够的，因为，当时并没有『自由的 Unix 内核』存在。所以这些软件只能在那些有专利的 Unix 平台上工作，直到 Linux 的出现。
>
> GNU 项目组曾经致力于开发一个名为"**GNU Hurd**"的微内核架构的类Unix操作系统内核，但由于微内核架构实现过于复杂且开发资源不足，始终没有开发成功。待到Linux内核诞生，Hurd就被完全搁置了且最终也没有开发成功。
>
> <span style=color:red;background:yellow>**1988年：POSIX 标准诞生**</span>
>
> POSIX 是 Portable Operating System Interface 的缩写，而 X 表示其对 Unix API 的传承。POSIX 这个名称是由 Richard Stallman 向 IEEE 协会提议的一个名字，它最初的名字为 IEEE-IX。
>
> POSIX 标准源于 1985 年的一个项目，该项目旨在保证各个操作系统之间的兼容性。也就是说，如果两个系统都遵循 POSIX 标准的话，那么在一个系统上能够运行的软件，在另一个系统上也可以运行。1988年，发布了 POSIX 标准的第一个版本。
>
> Linux 基本上逐步实现了 POSIX 兼容，但并没有参加正式的 POSIX 认证。微软的 Windows NT 声称部分实现了 POSIX 标准。
>
> <span style=color:red;background:yellow>**1991年：Linux 0.0.2 版发布**</span>
>
> 1988 年，Linus Torvalds 进入了赫尔辛基大学，并选读了计算机科学系。在读大学本科期间，他接触到了 Unix 系统。
>
> 当时整个赫尔辛基只有一部最新的 Unix 系统，同时仅提供 16 个终端 (Terminal)连接。早期的计算机仅有主机具有运算功能，Terminal 仅负责提供 Input/Output 而已。
>
> 这种情况，实在很难满足 Linus 的需求。不久之后，他就知道有一个类似 Unix 的操作系统，并且与 Unix 完全兼容，还可以在 Intel 386 机器上面运行 —— Minix。Linus Torvalds 贷款购买了一台 Intel 386 个人计算机，安装了 Minix 操作系统，并通过 Minix 操作系统附带的源代码，学习到了很多内核程序设计的理念。
>
> 由于 Andrew Tanenbaum 教授只愿意将 Minix 系统用于教学，不愿意强化系统功能，而 Linus Torvalds 是一个不服输充满探究精神的人，因此他萌生自己编写操作系统的想法。
>
> 而 GNU 计划产出的 Bash Shell 和 GCC 编译程序等自由软件，让 Linus 能够顺利的编写内核程序。最终，在 1991 年，他写出了能够在 Intel 386 计算机上运行的类Unix系统内核，并将其上传到网络上，提供免费下载。
>
> 当时 Linus Torvalds 是将该内核放在 Linux 目录下以供下载，所以该系统就从此被称为 Linux 系统。
>
> 至此，具有划时代意义的Linux操作系统诞生了。

# 操作系统内核和发行版

> _~Gn!~_
>
> 值得注意是，严格来说，1991年林纳斯·托瓦兹（Linus Torvalds）发布的Linux指的是操作系统的内核。
>
> <font color=red>**内核是操作系统的核心部分：**</font>
>
> 1. 向下层，内核提供硬件资源的管理。负责管理计算机的所有硬件资源，如CPU、内存和外部设备。
> 2. 向上层，内核独立于任何特定的用户界面和应用程序而存在，你可以把它看成是一个最底层的系统软件，为上层的应用提供基础服务。
>
> 了解操作系统内核后，我们还需要了解操作系统发行版的概念：
>
> 系统内核对于大多数用户而言，抽象层次都太低了，很难直接作为操作系统使用，缺乏很多必要的功能。比如：
>
> 1. 用户图形界面
> 2. 各种必要的应用程序(浏览器、文本编辑器、图像处理软件等)
> 3. 系统工具：用于系统设置和管理的工具，如网络配置、用户管理等。
> 4. ..
>
> 因此，在内核的基础上，结合这些用户层的软件和工具，我们才能得到一个完整的操作系统体验。<font color=red>**这就是操作系统的发行版。**</font>
>
> 不同的发行版针对不同的用途和用户群体可能进行了特别的优化和配置，但基于同一内核的不同发行版在本质上是一致的。
>
> 关于内核和发行版它们的关系可以总结成：
>
> 1. 如果将内核比作汽车的引擎，那么发行版就相当于一辆包含引擎并配备有座椅、轮胎和车身等部分的完整汽车。
> 2. 没有内核，发行版就无法运行。而没有发行版，内核对大多数用户来说并不能真正提供操作系统的完整服务。二者互相依赖，共同构成用户实际使用的操作系统。
>
> <span style=color:red;background:yellow>**因此，当我们谈论Linux时，我们可能指的是纯粹的Linux内核，也可能是基于该内核的完整操作系统发行版(通常指的是后者)。**</span>

# Linux的发行版本

> _~Gn!~_
>
> 1991年后，林纳斯发布Linux操作系统内核并开源后，涌现了大量的基于Linux内核的发行版。这里按照年份和特性总结一下：
>
> 1. **Slackware**：诞生于1993年。它是最古老的，且仍在维护的Linux发行版。
> 2. **Debian**：诞生于1993年，稍晚于Slackware。Debian以其社区驱动和稳定性著称。
> 3. **Red Hat Enterprise Linux (RHEL)**：诞生于1994年，由著名的Red Hat公司开发。面向企业级市场，强调稳定性和安全性。RHEL提供付费支持，并且在企业环境中广受欢迎。
> 4. **Fedora**：诞生于2003年。作为RHEL的上游源，Fedora完全免费，主要由社区维护，并得到Red Hat公司的支持。Fedora的特点是注重创新和尝试新鲜技术。
> 5. <span style=color:red;background:yellow>**Ubuntu：**</span>Ubuntu是公认提供最友好用户交互界面的Linux发行版本，诞生于2004年。Ubuntu适合桌面用户，特别是Linux新手。但它在云计算和服务器市场也有很强的存在感。
> 6. <span style=color:red;background:yellow>**CentOS：**</span>基于RHEL构建，但完全免费，同样诞生于2004年。它提供与RHEL相似的稳定性和安全性，适合需要企业级稳定性但没有预算的中小型企业。CentOS在当下的生产环境中，是最普遍的服务器操作系统。
>
> > **补充：查看Linux内核和发型版本**
> >
> > Linux内核是完全开源免费的，包括源码都可以在网络上进行下载查看。
> >
> > 你可以访问网站：[The Linux Kernel Archives](https://kernel.org/)来获取。
> >
> > ![Linux内核版本号-图](https://hixiaodong123.oss-cn-hangzhou.aliyuncs.com/typora/202402181414521.png#padding#600w)
> >
> > 公司生产环境使用Linux，往往会更多考虑稳定而不是尝试新特性功能，所以总是会选择`longterm`长期支持版本使用。
> >
> > 那我们怎么知道自己使用的Linux系统内核版本号呢？
> >
> > 这里讲一个shell命令——`uname-a`
> >
> > 该命令历史悠久来源于Unix，所有类Unix系统中都可以执行该shell命令。它的作用是显示系统的详细信息，比如内核版本，主机名，发行版本等。
> >
> > 比如参考下图：
> >
> > ![Linux内核版本号-图2](https://hixiaodong123.oss-cn-hangzhou.aliyuncs.com/typora/202402181422749.png#padding#600w)
> >
> > 如果你只想单独查询发行版本信息，可以使用命令——`cat /etc/issue`
> >
> > ![Linux内核版本号-图3](https://hixiaodong123.oss-cn-hangzhou.aliyuncs.com/typora/202402181425383.png#padding#600w)
> >
> > 以上。



# Linux成功的原因

> _~Gn!~_
>
> Linux之所以成功归根结底有两个原因：
>
> 1. 开源免费。这是Linux成功最核心的原因，开源任何人都可以查看、修改和改进其源代码。
>    1. Linux采用单内核架构实现更加简单直接，容易理解和开发。使得Linux在发布开源之初，就获得了极大的支持。
>    2. 实际上世界上很多公司都为Linux内核源码添砖加瓦过，比如Intel、IBM、小红帽、微软、谷歌、华为等
> 2. 遵循POSIX标准，提高了Linux系统的兼容性。
> 3. 确实很强大。从稳定性、灵活性、性能乃至于兼容性多个角度出发，Linux都是一款非常优秀的操作系统。
>
> 在2000年左右的时候，服务器端操作系统的主流选择还是Windows，但越来越多的公司开始无法忍受Windows Server的高额费用，随着Linux的发展成熟，Linux逐渐取代了Windows在服务器端的地位。
>
> 到如今，Linux已成为服务器端操作系统的霸主，尤其在涉及云计算，Web服务器，大数据处理服务器等领域时我们可以默认它就是Linux操作系统的。

# Linux系统层次结构

> _~Gn!~_
>
> Linux系统的结构设计沿袭于Unix，它是一个层次分明的类似"坚果"的结构，如下图所示：
>
> ![Linux内核结构-图](https://hixiaodong123.oss-cn-hangzhou.aliyuncs.com/typora/202402152237065.png#padding#400w)
>
> 我们之前在讲虚拟内存空间，JVM虚拟机时，都提到了这种分层的软件设计架构，这里Linux系统结构的设计同样是一种分层的思想。
>
> <span style=color:red;background:yellow>**分层的设计可以使得思考问题的复杂度降低，没有必要过度思考更深层次的问题。在计算机领域，有一句名言：没有解决不了的问题，如果有，那就加一层。**</span>
>
> Linux系统的层次结构，从内到外分别是"内核 --> 系统调用层 --> 库函数层 --> 应用层"。下面对这些层次进行逐一的讲解：
>

## 内核与系统调用层

> _~Gn!~_
>
> 内核(Kernel)和系统调用这两层要合起来一起看：
>
> 1. 内核的作用是管理计算机的一切硬件资源，比如CPU、内存、外部设备等等。所以任何需要访问或控制硬件资源的操作，最终都需要与内核交互来实现。比如：
>
>    1. 文件系统管理
>    1. 外部设备管理（如键盘、显示器等）
>    1. 进程和线程资源管理
>    1. 内存管理(动态内存分配)
>    1. 网络通信
>
> 2. 但也不是所有的操作都需要和内核进行交互，当仅涉及数据在内存中的变化时，一般不需要内核参与：
>    1. 一般的函数调用
>    1. 加减乘除算术运算
>    1. 变量的声明、赋值等基本变量操作
>    1. 程序的流程控制
>
> 内核是不能让外界随意访问的，外界只能通过内核主动暴露给外界的接口去进行内核级别的操作，这就是<font color=red>**"系统调用"**</font>。
>
> "系统调用"为程序员编写应用程序，进行硬件层面的控制提供了可能。Linux操作系统的"系统调用"接口是以C语言风格暴露的。
>
> <span style=color:red;background:yellow>**"系统调用"是用户应用程序和内核交互，控制硬件资源的一种常见手段，也是最直接和基本的手段。**</span>
>
> 常见的系统调用有：文件操作，进程和线程控制，网络通信(一系列系统调用合集)等。在后续的课程中，这些我们都会学习。
>
> 下面的例子可以帮助大家通俗的理解内核和系统调用的关系：
>
> 你可以把内核看成是银行的金库，储户的钱都是存在金库里的。但没有人可以直接去金库取钱，都需要通过银行柜员间接的从金库里取钱。
>
> 这个例子里，金库就是操作系统内核，它负责实现真正的功能，但外界不能直接交互内核。柜员就相当于系统调用，外界需要通过柜员间接地在金库里取钱。

## 库函数层

> _~Gn!~_
>
> 在内核与系统调用层上面，是库函数层。
>
> 所谓库函数，可以将系统调用进行封装，使得程序员可以更方便、更简单乃至于更高效的使用系统调用。
>
> 关于系统调用和库函数的区别，如下：
>
> 1. 系统调用仅和操作系统内核有关，不同操作系统内核具有不同的系统调用接口。
> 2. <span style=color:red;background:yellow>**库函数是建立在系统调用之上的高级编程接口，它们封装了系统调用，提供了更方便实用的接口。不同的编程语言，一般具有不同的库函数实现，库函数是编程语言的概念。**</span>
> 3. 库函数的功能和实现可以五花八门，但我们在学习一门编程语言时，更关注它的"标准库函数"。编程语言的标准库函数，为程序员提供了高效、可靠的方式来解决实现常见的编程任务。
> 4. 而且标准库函数还实现了跨平台性，在不同平台系统下，系统调用的接口是不同的，但不同平台系统的标准库函数至少在程序员使用起来是没有区别的。(用起来一样，但底层实现不同，这叫隐藏实现细节)
>
> <span style=color:red;background:yellow>**库函数也提供了一种用户应用程序和内核交互的方式，但库函数不是一种全新的方式，而是对系统调用的封装。**</span>封装的目的是为了实现跨平台的统一性，使用的方便性等。
>
> > **举例说明：库函数和系统调用的区别**
> >
> > 在Linux操作系统下，进行IO操作有两个非常基础和重要的系统调用函数：
> >
> > 1. read函数
> > 2. write函数
> > 3. 这两个函数都是以C语言风格暴露的系统调用接口，它们的具体使用我们后续再讲。
> >
> > 在Windows操作系统下，也有类似的两个函数(Windows系统API，作用和Linux系统调用类似)：
> >
> > 1. ReadFile函数
> > 2. WriteFile函数
> >
> > 标准库函数，以C语言为例子：
> >
> > 诸如：`fwrite`、`fread`、`scanf`、`printf`等涉及到数据输入输出的库函数，底层实现都是基于以上两种情况：
> >
> > 1. 如果在Linux下使用这些库函数，最终就是利用Linux系统调用的read和write函数
> > 2. 如果在Windows下使用这些库函数，最终就是利用Windows系统API的ReadFile函数和WriteFile函数。
> >
> > 当然，不管在什么系统平台下，这些库函数的使用对于程序员而言都是一样的，而且使用简单，功能完善。这就是库函数的作用。

## 应用层和shell

> _~Gn!~_
>
> 库函数层再往上就到了应用层，也就是一般的用户应用程序。用户应用程序在一定前提下可以直接进行系统调用和内核交互，也可以调用库函数，方便高效的实现一些功能。
>
> 应用层的程序中，shell是一个比较特殊的存在：
>
> 1. 首先shell是一个命令行解释器，它读取用户输入执行命令，shell解释这些命令并调用相应的程序来执行任务，执行完毕后又继续等待用户的下一次输入。
> 2. 具体到底层，shell可能调用库函数来实现功能，也可能直接进行系统调用来实现功能。
>
> 所以，<span style=color:red;background:yellow>**shell又提供了一种用户应用程序和内核交互的方式，它可能是系统调用的封装，也可能是库函数的封装。**</span>
>
> shell的版本有很多，在Linux下默认的shell程序是`Bash`，它是GNU项目的产品之一，也是目前使用最广泛的shell，在随后的课程中我们都将使用Bash。（MacOS从19年开始选择`Zsh`作为默认的shell程序。）
>
> Bash作为一个应用程序，它的可执行程序的文件目录是：`/bin/bash`

## 学习路线

> _~Gn!~_
>
> 搞清楚Linux系统的层次结构后，我们知道了三种和系统内核交互的方式：
>
> 1. 通过系统调用和内核直接交互，这是我们后续就要学习的内容——Linux系统编程。
> 2. 通过封装系统调用的库函数和内核间接交互，在C语言阶段我们已经学过很多这样的库函数了。比如printf、scanf、malloc等。
> 3. 通过封装系统调用或者库函数的shell和内核间接交互，这是接下来的几天需要学习的内容。

# XShell的原理

> _~Gn!~_
>
> 要想搞清楚XShell的原理，我们先要了解一个网络协议：SSH
>
> SSH是一种远程连接通信的网络协议，基于SSH协议我们可以实现安全的远程登陆，从而访问和管理远程计算机。
>
> SSH是一套基于C/S架构(客户端/服务端架构)的模型，即包含SSH服务端以及SSH客户端。客户端可以通过一定的方式进行远程连接服务端，而且一台服务端支持多台客户端连接。
>
> ![SSH连接模型-图](https://hixiaodong123.oss-cn-hangzhou.aliyuncs.com/typora/202402181353293.png#padding#600w)
>
> 在上一小节配置环境时，我们使用过以下指令：
>
> ```` shell
> sudo apt install ssh
> ````
>
> 这条指令的含义是在Ubuntu上安装SSH服务组件，也包括一个SSH服务端`sshd`，使用下面的指令就可以看到这个服务端：
>
> ```` shell
> ps -elf | grep sshd
> ````
>
> 所以在远程连接中，Ubuntu虚拟机就充当SSH服务端，而XShell软件就是一种SSH客户端，通过SSH协议，我们可以用XShell远程连接访问和管理Ubuntu。
>
> 当然市面上SSH客户端有很多，图形界面和使用方式都不尽相同，但它们使用的本质原理是一样的：通过SSH协议远程连接，然后一般都是基于`Bash`的shell来操作远程Liunx计算机。
>
> 除此之外，我们在上课时使用的软件"WinSCP"本质上也是一种基于基于SSH协议的文件传输客户端，所以它的连接和使用方式存在和XShell类似的地方，但还是稍有区别：
>
> 1. XShell主要用于执行远程命令，所以它默认就启动Bash这样的shell进程，解释执行用户远程输入的指令。
> 2. WinSCP主要用于文件传输，所以提供一个图形界面使得用户可以更好的进行文件复制、下载等操作。同时，它还使用SSH协议的文件传输子协议（如SFTP或SCP）来确保数据传输的安全性。

# The End