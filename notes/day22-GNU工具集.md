- 预处理指令:
    #ifndef:
    #ifdef:
    #if:
这些都是条件编译--通过预处理指令控制代码是否能进入编译；
- 相对路径和绝对路径：
    - 绝对路径：
    - 相对路径：
    相对路径的关键点是相对路径的起点；
- debug的大致思路:
    1.大致扫一遍代码，检查是否有明显的错误;
    2.关注高风险区域，例如指针，链表等区域；
    3.采用注释法和取消注释的方法检查；
    4.如果发现了崩溃的位置，顺着数据链检查，数据来自哪里，流向哪里；
- gdb调试：1.编译时在后面加-g
         2.gdb xxx 或者gdb -> file xxx;
- gdb的相关指令: ...
                print xx = xx可以修改值
                查看内存窗口: x/(内存单元个数)(一个内存单元的大小)数组名/指针/地址值...
                内存地址的输出格式和内容有...
- 复习一下Linux的目录结构和各个目录的分类...
- 调试bt用法:...
- 指针数组相关知识：...
- 输入命令行仓库...
- int main(int avgc, char *avgv) ...
- gdb传参：...
- 调试Coredump文件...
    使用方式 gdb xx + Coredump 生成的黑盒;
- 库文件是什么：... 简单说库文件就是已经写好的轮子；
- 静态库:
    优/缺点
    生成静态库和使用方式：
    静态库的后缀有哪些：
- 动态库:
    优/缺点
    生成动态库和使用方式：
    动态库的后缀有哪些:
- Makefile文件：
    Makefile文件名称固定为makefile/Makefile;
    Makefile的基本组成单元是规则;
    注：command的开头必须是一个指标符
    Makefile的增量编译:...其基本原理是通过...时间实现
    伪目标：...
    makefile的结构和位置
    makfile自动变量...
    makefile的相关参数...
